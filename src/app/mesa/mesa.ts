export class Mesa {
    id: number;
    numero: string;
    estado: boolean;

    constructor(id: number, numero: string, estado: boolean){
        this.id = id;
        this.numero = numero;
        this.estado = estado;
    }
}